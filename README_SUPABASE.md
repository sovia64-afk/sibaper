# SIBAPER — Supabase Ready

This archive is the complete `website_instansi` source supplied in the conversation, preserved visually, with Supabase authentication/setup added.

Read `SUPABASE_SETUP.md` and run `SUPABASE_SCHEMA.sql` before deploying.
